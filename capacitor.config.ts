import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inmobitt-cliente',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
